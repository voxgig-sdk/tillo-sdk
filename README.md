# Tillo SDK

Tillo Rewards API client, generated from the OpenAPI spec.

List Tillo brands and float balances, and issue digital gift cards.

Learn more about Tillo Rewards API at [app.tillo.io](https://app.tillo.io).

This is an unofficial SDK for the Tillo Rewards public API, generated by Voxgig with [`@voxgig/sdkgen`](https://github.com/voxgig/sdkgen). It is not affiliated with, endorsed by, or sponsored by the upstream API provider.

Learn more about Voxgig SDKs at [voxgig.com/sdk](https://voxgig.com/sdk/).

> TypeScript, Python, PHP, Golang, Lua, JavaScript SDKs, a CLI with an interactive REPL, and an MCP server for AI agents — all generated from one OpenAPI spec by [@voxgig/sdkgen](https://github.com/voxgig/sdkgen).

## Entities, not endpoints

This SDK exposes the API as a small set of **semantic entities** — Brand, Dgc and Float — that you
call directly, instead of assembling URL paths and query strings. Entities are
**Capitalised** to mark them as the primary surface, each with the operations they
support (`list`, `create`):

```ts
const client = new TilloSDK()
const items = await client.Brand().list()
```

Thinking in entities keeps the mental model small — for people and AI agents alike —
rather than reasoning about raw HTTP routes and query parameters.

## Offline unit testing

Every SDK ships a built-in **test mode** that swaps the HTTP transport for
an in-memory mock, so your unit tests run fully offline — no server, no
network, and no credentials:

### TypeScript

```ts
// The offline mock starts EMPTY — seed it with the records the test needs.
// Shape: { entity: { <entity-name>: { <id>: <record> } } }
const client = TilloSDK.test({
  entity: {
    brand: {
      test01: { id: 'test01' },
    },
  },
})
const brands = await client.Brand().list()
// brands is an array of Brand entities, populated with mock data
// — call brands[0].data() for the record itself
console.log(brands)
```

### Python

```python
client = TilloSDK.test()
brands = client.Brand().list()
print(brands)
```

### PHP

```php
// Seed fixture data so offline calls resolve without a live server.
$client = TilloSDK::test([
    "entity" => ["brand" => ["test01" => []]],
]);
$brands = $client->Brand()->list();
```

### Golang

```go
client := sdk.Test()
result, err := client.Brand(nil).List(
    nil, nil,
)
```

### Lua

```lua
local client = sdk.test()
local results, err = client:Brand():list()
```

### JavaScript

```js
const client = TilloSDK.test()
const brands = await client.Brand().list()
// brands is an array of entities, populated with mock data
// — call brands[0].data() for the record itself
console.log(brands)
```

## Packages

| Language | Package | Install |
| --- | --- | --- |
| TypeScript | `@voxgig-sdk/tillo` | publish pending — [install from git tag](https://github.com/voxgig-sdk/tillo-sdk/releases) |
| Python | `voxgig-sdk-tillo` | publish pending — [install from git tag](https://github.com/voxgig-sdk/tillo-sdk/releases) |
| PHP | `voxgig-sdk/tillo` | publish pending — [install from git tag](https://github.com/voxgig-sdk/tillo-sdk/releases) |
| Golang | `github.com/voxgig-sdk/tillo-sdk/go` | `go get github.com/voxgig-sdk/tillo-sdk/go@latest` |
| Lua | `voxgig-sdk-tillo` | publish pending — [install from git tag](https://github.com/voxgig-sdk/tillo-sdk/releases) |
| JavaScript | `@voxgig-sdk/tillo-js` | publish pending — [install from git tag](https://github.com/voxgig-sdk/tillo-sdk/releases) |
| Go CLI | `github.com/voxgig-sdk/tillo-sdk/go-cli` | `go install github.com/voxgig-sdk/tillo-sdk/go-cli/cmd/tillo@latest` |
| Go MCP server | `github.com/voxgig-sdk/tillo-sdk/go-mcp` | `go get github.com/voxgig-sdk/tillo-sdk/go-mcp@latest` |

## Quickstart

### TypeScript

```ts
import { TilloSDK } from '@voxgig-sdk/tillo'

const client = new TilloSDK({
  apikey: process.env.TILLO_APIKEY,
})

// List all brands (returns BrandEntity[] — .data() for the record)
const brands = await client.Brand().list()
for (const brand of brands) {
  console.log(brand)
}
```

See the [TypeScript README](ts/README.md) for the full guide.

## Surfaces

| Surface | Path |
| --- | --- |
| **SDK** (TypeScript, Python, PHP, Golang, Lua, JavaScript) | `ts/` `py/` `php/` `go/` `lua/` `js/` |
| **CLI** | `go-cli/` |
| **MCP server** | `go-mcp/` |

## Use it from an AI agent (MCP)

The generated MCP server exposes every operation in this SDK as an
[MCP](https://modelcontextprotocol.io) tool that Claude, Cursor or Cline
can call directly. Build and register it:

```bash
cd go-mcp && go build -o tillo-mcp .
```

Then add it to your agent's MCP config (Claude Desktop, Cursor, etc.):

```json
{
  "mcpServers": {
    "tillo": {
      "command": "/abs/path/to/tillo-mcp"
    }
  }
}
```

## Entities

The API exposes 3 entities:

| Entity | Description | API path |
| --- | --- | --- |
| **Brand** | The Brand entity (list). | `/brands` |
| **Dgc** | The Dgc entity (create). | `/digital/issue` |
| **Float** | The Float entity (list). | `/check-floats` |

The operations available across these entities are **list**, **create** — see each entity's
own list above for exactly which it supports.

## Quickstart in other languages

### Python

```python
import os
from tillo_sdk import TilloSDK

client = TilloSDK({
    "apikey": os.environ.get("TILLO_APIKEY"),
})

# List all brands (returns a list, raises on error)
brands = client.Brand().list()
for brand in brands:
    print(brand)
```

### PHP

```php
<?php
require_once 'tillo_sdk.php';

$client = new TilloSDK([
    "apikey" => getenv("TILLO_APIKEY"),
]);

// List all brands (returns an array; throws on error)
$brands = $client->Brand()->list();
print_r($brands);
```

### Golang

```go
import sdk "github.com/voxgig-sdk/tillo-sdk/go"

client := sdk.NewTilloSDK(map[string]any{
    "apikey": os.Getenv("TILLO_APIKEY"),
})

// List all brands
brands, err := client.Brand(nil).List(nil, nil)
if err != nil {
    panic(err)
}
fmt.Println(brands)
```

### Lua

```lua
local sdk = require("tillo_sdk")

local client = sdk.new({
  apikey = os.getenv("TILLO_APIKEY"),
})

-- List all brands
local brands, err = client:Brand():list()
print(brands)
```

### JavaScript

```js
const { TilloSDK } = require('@voxgig-sdk/tillo-js')

const client = new TilloSDK({
  apikey: process.env.TILLO_APIKEY,
})

// List all brands (returns an array)
const brands = await client.Brand().list()
for (const brand of brands) {
  console.log(brand)
}
```

## Direct and prepare

For endpoints the entity model doesn't cover, use the low-level methods:

- **`direct(fetchargs)`** — build and send an HTTP request in one step.
- **`prepare(fetchargs)`** — build the request without sending it.

Both accept a map with `path`, `method`, `params`, `query`,
`headers`, and `body`. See the [How-to guides](#how-to-guides) below.

## How-to guides

### Make a direct API call

When the entity interface does not cover an endpoint, use `direct`:

**TypeScript:**
```ts
const result = await client.direct({
  path: '/api/resource/{id}',
  method: 'GET',
  params: { id: 'example' },
})
if (result instanceof Error) {
  throw result
}
console.log(result.data)
```

**Python:**
```python
result = client.direct({
    "path": "/api/resource/{id}",
    "method": "GET",
    "params": {"id": "example"},
})
```

**PHP:**
```php
$result = $client->direct([
    "path" => "/api/resource/{id}",
    "method" => "GET",
    "params" => ["id" => "example"],
]);
```

**Go:**
```go
result, err := client.Direct(map[string]any{
    "path":   "/api/resource/{id}",
    "method": "GET",
    "params": map[string]any{"id": "example"},
})
if err != nil {
    panic(err)
}
fmt.Println(result)
```

**Lua:**
```lua
local result, err = client:direct({
  path = "/api/resource/{id}",
  method = "GET",
  params = { id = "example" },
})
```

**JavaScript:**
```js
const result = await client.direct({
  path: '/api/resource/{id}',
  method: 'GET',
  params: { id: 'example' },
})
if (result instanceof Error) {
  throw result
}
console.log(result.data)
```

## Advanced

> Everyday use only needs the sections above. This explains the internals
> behind every call — relevant when writing custom features.

Every SDK call runs the same five-stage pipeline:

1. **Point** — resolve the API endpoint from the operation definition.
2. **Spec** — build the HTTP specification (URL, method, headers, body).
3. **Request** — send the HTTP request.
4. **Response** — receive and parse the response.
5. **Result** — extract the result data for the caller.

A feature hook fires at each stage (e.g. `PrePoint`, `PreSpec`,
`PreRequest`), so features can inspect or modify the pipeline without
forking the SDK.

### Features

| Feature | Purpose |
| --- | --- |
| **TestFeature** | In-memory mock transport for testing without a live server |

Pass custom features via the `extend` option at construction time.

## Per-language documentation

- [TypeScript](ts/README.md)
- [Python](py/README.md)
- [PHP](php/README.md)
- [Golang](go/README.md)
- [Lua](lua/README.md)
- [JavaScript](js/README.md)

## Upstream API

This SDK is generated from the upstream OpenAPI specification. It is an
unofficial client and is not affiliated with the API provider.

The OpenAPI spec(s) this SDK was generated from are kept in the
[`.sdk/def/`](.sdk/def/) folder.

- Upstream API: [https://app.tillo.io](https://app.tillo.io)

## Security

Please report security issues to security@voxgig.com. See [SECURITY.md](SECURITY.md).
Do not open public issues for suspected vulnerabilities.

---

Generated from the Tillo Rewards API OpenAPI spec by [@voxgig/sdkgen](https://github.com/voxgig/sdkgen).
